reachy-mini
openai-whisper
pyttsx3
sounddevice
numpy
soundfile
opencv-python
facenet-pytorch
hsemotion-onnx
openai

# Realtime API (full-duplex voice)
websockets

# Robot teleoperation API
flask
flask-cors

# YouTube music streaming
yt-dlp

# Voice cloning (optional — set ELEVENLABS_API_KEY to activate)
elevenlabs

# RAG memory
# OpenAI embeddings preferred; hash-based fallback if no API key

# Postgres/Supabase support (optional — set SUPABASE_DB_URL to activate)
psycopg2-binary

# Fall detection
mediapipe

# Smart home (optional — needs Home Assistant)
# No extra deps, uses urllib

# Vitals monitoring (optional — BLE devices)
# bleak

# Testing
pytest
pytest-cov
